# 管理员账号（用于登录控制面板）
ADMIN_USERNAME=
ADMIN_PASSWORD=

# 加密密钥（用于加密存储 AI API Key，生产环境必须配置）
# 生成方式：openssl rand -base64 32 或 node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# 长度至少 32 字符
ENCRYPTION_SECRET=
